#include "Passenger.h"
#include <iostream>
#include <cstdlib>

void Passenger::inputDetails(Ticket& ticket) {
    // Implementation remains the same
}

void Passenger::displayAvailableSeats(const Ticket& ticket) {
    // Implementation remains the same
}

// Implement remaining member functions as in the original code
