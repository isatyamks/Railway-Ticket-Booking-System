#include "RailwayMember.h"
#include <iostream>
#include <fstream>

RailwayMember::RailwayMember(int max) : maxTickets(max) {}

bool RailwayMember::bookTicket(std::vector<Ticket>& tickets, const Ticket& newTicket) {
    // Implementation remains the same
}

void RailwayMember::writePassengerDetails(const Ticket& ticket) {
    // Implementation remains the same
}

void RailwayMember::displayBookedTicket(const std::vector<Ticket>& tickets, const std::string& pnr) {
    // Implementation remains the same
}

void RailwayMember::displayTicketDetails(const Ticket& ticket) {
    // Implementation remains the same
}
